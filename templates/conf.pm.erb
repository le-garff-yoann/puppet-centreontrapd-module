# GENERATED by 'centreontrapd' Puppet class

$centreon_config = {
    'VarLib'            => '<%= @varLib %>',
    'CentreonDir'       => '<%= @centreonDir %>',
    'centreon_db'       => '<%= @centreonDb %>',
    'centstorage_db'    => '<%= @centstorageDb %>',
    'db_host'           => '<%= @dbType == "SQLite" ? nil : @dbHost %>',
    'db_user'           => '<%= @dbType == "SQLite" ? nil : @dbUser %>',
    'db_passwd'         => '<%= @dbType == "SQLite" ? nil : @dbPasswd %>',
    'db_type'           => '<%= @dbType %>'
};

# Central or Poller ?

$instance_mode = '<%= @isPoller ? "central" : "poller" %>';

# Centreon Centcore Command File

$cmdFile = '/var/lib/centreon-engine/rw/centengine.cmd';

# Deprecated format of Config file.

$mysql_user = '<%= @dbType == "SQLite" ? nil : @dbUser %>';
$mysql_passwd = '<%= @dbType == "SQLite" ? nil : @dbPasswd %>';
$mysql_host = '<%= @dbType == "SQLite" ? nil : @dbHost %>';
$mysql_database_oreon = '<%= @centreonDb %>';
$mysql_database_ods = '<%= @centstorageDb %>';

1;
